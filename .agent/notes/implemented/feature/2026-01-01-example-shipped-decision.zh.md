# Agent Note: 示例已交付决策

Status: implemented

[English](2026-01-01-example-shipped-decision.md) | 中文

> 模板示例——请用真实的决策记录替换本文件。将其重命名为
> `implemented/{class}/yyyy-mm-dd-topic-title.md`，使用真实的首提日期，并编写对应的
> `.zh.md` 与 `.i18n.yaml` 对侧文件。

## 问题

说明变更为何重要，写法不依赖解决方案即可独立成文。

## 决策

以现在时态描述已交付的现实。在改动它们的同一个变更中保持路径、符号、默认值与机制同步；不要追加变更历史。

## 曾考虑的替代方案

**备选方案一。** 说明该备选方案及其落选原因。

## 影响

记录权衡付出的代价**与**带来的收益。
